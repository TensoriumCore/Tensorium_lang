field cov_tensor2 T[i,j]
field scalar s

simulation {
  dimension = 2
  resolution = [8,8]
  time { dt = 0.1 integrator = euler }
  spatial { scheme = fd derivative = centered order = 2 }
}

evolution CanonTraceForm {
  dt s = trace(T[i,i])
}
