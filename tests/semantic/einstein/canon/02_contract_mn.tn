field vector A[m]
field covector B[m]
field scalar s

simulation {
  dimension = 1
  resolution = [8]
  time { dt = 0.1 integrator = euler }
  spatial { scheme = fd derivative = centered order = 2 }
}

evolution CanonContractMN {
  dt s = contract(A[m] * B[m])
}
